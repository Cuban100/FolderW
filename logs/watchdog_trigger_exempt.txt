# Files listed here are still fully included in every backup -- this
# list only controls whether writing to them, by itself, is allowed to
# schedule a new backup run. Confirmed via lsof/stat/systemctl (not just
# inferred) that each entry is rewritten continuously by a long-running
# host or container service regardless of any real user activity, which
# meant it alone was enough to keep the watchdog's 300s ceiling firing
# around the clock. Add entries here only once you've actually confirmed
# a file behaves this way -- see rsync_event_handler.py's should_ignore().
telegraf.log

# FolderW's own working directory, including folderw.db. Confirmed live:
# during a single backup run, the app's own progress-reporting writes to
# folderw.db (throttled to once per 2s, but a run can take several
# minutes) were enough on their own to re-arm the 300s trigger ceiling
# before the run even finished -- producing a new backup right after the
# previous one, indefinitely, with zero real user activity involved.
# folderw.db is still copied into every backup like any other file; it
# just can't be the reason one gets scheduled.
FolderW
